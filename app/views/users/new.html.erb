<h1>All Users</h1>

<% @users.each do |user| %>
  <p>
    <strong>Name:</strong> <%= user.name %> |
    <strong>Email:</strong> <%= user.email %> |
    <%= link_to "View Details", user_path(user) %>
  </p>
<% end %>
